import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Blemith & Blemith | Andamios en Graneros - Arriendo y Venta",
  description: "Empresa líder en arriendo y venta de andamios en Graneros. Ofrecemos andamios tradicionales y certificados Layher para todo tipo de proyectos en la Región de O'Higgins.",
  icons: {
    icon: "/images/Logo%20copy.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
